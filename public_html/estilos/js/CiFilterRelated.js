 /* @author Damián Esteban Blanc Perez - damian.blanc@t-systems.es
 /* @version 1.0 -04/04/2018
 */ 


 /***********************************************************************************
	Clean CI filters
 */



$('div#divso').click(function(){

	alert("LOW");
});

//
/*$("#so").click(function(){
 		var index = -1;
		
		for (var i = 0; i < searchMultifields.length; i++) {
			
			if(searchMultifields[i].name == "so"){
				index = i;
				
			}
				
		}

		searchMultifields.splice(index, 1);

		for(var a = 0; a < searchMultifields.length; a++)
		{
			searchValue += searchMultifields[a].val + " ";
		}

		CERT.values.queryResult.fnFilter(searchValue);

		//TODO posicionar el combo en index 0

 });

 /*$(".gp").click(function(){

 	var index = -1;
		
		for (var i = 0; i < searchMultifields.length; i++) {
			
			if(searchMultifields[i].name == "gp"){
				index = i;
				
			}
				
		}

		searchMultifields.splice(index, 1);

		for(var a = 0; a < searchMultifields.length; a++)
		{
			searchValue += searchMultifields[a].val + " ";
		}

		CERT.values.queryResult.fnFilter(searchValue);

 	
 });
 $(".cli").click(function(){

 	var index = -1;
		
		for (var i = 0; i < searchMultifields.length; i++) {
			
			if(searchMultifields[i].name == "cli"){
				index = i;
				
			}
				
		}

		searchMultifields.splice(index, 1);

		for(var a = 0; a < searchMultifields.length; a++)
		{
			searchValue += searchMultifields[a].val + " ";
		}

		CERT.values.queryResult.fnFilter(searchValue);


 	
 });
 $(".ent").click(function(){

 	var index = -1;
		
		for (var i = 0; i < searchMultifields.length; i++) {
			
			if(searchMultifields[i].name == "ent"){
				index = i;
				
			}
				
		}

		searchMultifields.splice(index, 1);

		for(var a = 0; a < searchMultifields.length; a++)
		{
			searchValue += searchMultifields[a].val + " ";
		}

		CERT.values.queryResult.fnFilter(searchValue);

 	
 });*/